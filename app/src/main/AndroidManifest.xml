<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nasser.tips">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Health Tips"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".firstMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".secondMain" />
        <activity android:name=".HealthTip1" />
        <activity android:name=".HealthTip2" />
        <activity android:name=".HealthTip3" />
        <activity android:name=".HealthTip4" />
        <activity android:name=".HealthTip5"></activity>
    </application>

</manifest>